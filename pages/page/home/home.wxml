<login id="login"></login>
<!-- 提示 -->
<mes show="{{show}}" bind:sureSelectArea="sureSelectAreaListener">
  <view>
    <text>加商友合作可享批发价</text>
    <button bindtap='confirm'>确定</button>
  </view>
</mes>
<view class="goods-ser">
  <navigator url="../../admin/serStatus/serStatus">
    <input placeholder="搜索店铺内商品" disabled='disabled' bindconfirm="searchBtn" value="{{value}}" />
  </navigator>
  <image class="home-ser" src="/image/search1.png"></image>
  <view class='home-img home-img1'>
    <navigator url="../derm/derm">
      <image src="/image/xcx.png"></image>
    </navigator>
  </view>
  <view class='home-img'>
    <navigator url="../details/details">
      <image src="/image/youbao.png"></image>
    </navigator>
  </view>
</view>
<view class='banner'>
  <view class='hm-fx' wx:if='{{limitShow==2}}'>
    <button class='shre_btn' open-type='share'>
      <image src="/image/fx1.png"></image>分享</button>
  </view>
  <image src="{{baseUrl}}{{coverUrl}}" class="slide-image" mode="widthFix" />
  <view class="ym_sydtstore">
    <view class="ym_sydtlogo">
      <navigator  class='logoimg' url="../mes/mes?code={{limitShow}}">
        <image mode="aspectFill" src="{{baseUrl}}{{store.logo}}"/>  
      </navigator>
    </view>
    <view class="ym_sydtdmgzs">
      <view class="store_name">{{store.storeName}}
        <view>{{store.storeNature}}</view>
        <image wx:if='{{limitShow==2}}' bindtap='editDp' src="/image/edit1.png" />
      </view>
      <image class='logoimg' src="{{baseUrl}}{{store.mallLogo}}" />
      <text>{{store.mallName}}</text>
    </view>
  </view>
  <button class="ym_sydtgzbtn" hidden='{{likeShow}}' bindtap='likeStore'>+ 关注</button>
  <button class="ym_sydtgzbtn" hidden='{{!likeShow}}' bindtap='deteleLikeStore'>已关注</button>
</view>
<view class='add-firend'  wx:if='{{limitShow!=3}}' bindtap='addTip'><view>商</view>点此加为商友，即刻查看批发价！<image src='/image/down.png'></image></view>
<!-- <view class='add-firend add-firend1' wx:if='{{limitShow==1}}'  hidden='{{!isShow}}'><view>验</view>商友关系验证中，通过后即可查看批发价！<image src='/image/down.png'></image></view> -->
<view class='add-firend add-firend2' wx:if='{{limitShow==3}}'><view>友</view>已成商友，现可查看批发价！<image src='/image/sy.png'></image></view>
<!-- <view class='add-firend add-firend3' wx:if='{{limitShow==3}}'><view>混</view>全场满3000元可混批！</view> -->
<view class="swiper-tab">
  <view class="swiper-tab-item {{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav">综合
    <view></view>
  </view>
  <view class="swiper-tab-item {{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav">销量
    <view></view>
  </view>
  <view class="swiper-tab-item {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">价格
    <image src="/image/sort.png" wx:if="{{currentTab!=2}}"></image>
    <image src="/image/sort1.png" wx:if="{{currentTab==2}}"></image>
    <view></view>
  </view>
</view>
<view class="result result-box" wx:if="{{result.length > 0}}">
  <view class='goods-total'>
    <text>{{result.length}}</text>
    <view></view>
    <text>{{totalCount}}</text>
  </view>
  <scroll-view scroll-y="true" style="height:100%;" bindscrolltolower="bindDownLoad">
    <view class='flex-boxx'>
      <view class="result-list" wx:for="{{result}}" wx:key="index">
        <view>
          <navigator url="../goodsDetails/goodsDetails?goodsId={{item.id}}">
            <view class='ho-img'>
              <image src="{{baseUrl}}{{item.mainImgUrl}}" mode="aspectFill"></image>
            </view>
          </navigator>
          <view class="result-text">
            <text>{{item.name}}</text>
          </view>
          <view class="result-text result-text1">
            零售
            <text>￥ {{item.sellPrice}}</text>
            <view>
              批发
              <text class='h-font' wx:if='{{limitShow!=1}}'>￥ {{item.wholesalePrice}}</text>
              <image wx:if='{{limitShow==1}}' src='/image/wh.png'></image>
              <image wx:if='{{limitShow==2}}' class='h-img' data-id='{{item.id}}' bindtap='editFun' src="/image/edit2.png" />
            </view>
          </view>
        </view>
      </view>
    </view>
    <view class='nomore'>优生活技术支持</view>
  </scroll-view>
</view>
<view class="result-null" wx:else>
  <view class="nomore">
    <view>
      <image src="/image/nohome.png"></image>
    </view>
    暂时还没有上架商品
  </view>
</view>
<view class='showHide' hidden='{{showHide}}'>
  <view class='sh-cont'>
    <view>分享</view>
    <view bindtap='topGoods'>置顶</view>
    <view bindtap='editGoods'>编辑</view>
    <view bindtap='upGoods'>下架</view>
    <view bindtap='closeShow'>取消</view>
  </view>
</view>
<view class='showHide' hidden='{{showDp}}'>
  <view class='sh-cont'>
    <view class='sh-title'>更换小云店封面</view>
    <view bindtap="chooseImage">拍照</view>
    <view bindtap="chooseImage">从手机相册选取</view>
    <view bindtap='closeShow'>取消</view>
  </view>
</view>
<view hidden='{{indexEmpty}}'>
  <no></no>
</view>